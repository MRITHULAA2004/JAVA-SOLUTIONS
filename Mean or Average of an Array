class Solution {
    public static int findMean(int[] arr) {
        // code here
        int n=arr.length;
        double avg=arr[0];
        for(int i=1;i<n;i++){
            avg+=arr[i];
            
        }
        avg=(avg/n);
        return (int)Math.floor(avg);
    }
};
